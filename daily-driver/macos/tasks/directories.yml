- name: Create home directories
  ansible.builtin.file:
    path: "{{ home_dir }}/{{ item }}"
    state: directory
    mode: "0755"
  loop: "{{ directories_to_create }}"
  tags:
    - directories
    - install
